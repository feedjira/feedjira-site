- wrap_layout :default do
  %article#post
    %header
      %h1= current_article.title
      %p
        Posted by
        %strong Jon Allured
        on
        %strong #{current_article.date.strftime('%B %e, %Y')}
    = yield
